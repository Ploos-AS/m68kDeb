# M1.1 qualification — current Debian Ports/m68k rootfs

Status: **PASS**

## Qualified commit

`593988ce4e00d3956c02dc50ca8c9b122be3dc2b`

## GitHub Actions evidence

- Workflow: `M1.1 rootfs qualification`
- Run: `34248096402`
- Result: success
- Rootfs qualification step: success
- Artifact upload: success

## Rootfs result

The qualification run built a current Debian Ports/m68k root filesystem with `mmdebstrap` under QEMU user-mode binfmt emulation.

Artifact bundle:

`m1-1-rootfs-593988ce4e00d3956c02dc50ca8c9b122be3dc2b`

GitHub artifact id: `10065037433`

Artifact ZIP digest reported by GitHub:

`sha256:48c34e3bb1485aabaead65cf1e57ac2a33f57cd165335f833eb2ed3015bcab79`

The rootfs tarball generated by the qualification pipeline is approximately 37.6 MB compressed. The previous run, using the same builder path before the final bookkeeping fix, produced SHA-256 `2703332ba3f00f93f39bf15fa465b81aa987117d9efe49c80072666f40e4ca55`; M1.1 qualification relies on the successful run's archived evidence rather than assuming byte-for-byte identity across rolling Debian Ports snapshots.

## What is qualified

- Debian Ports `unstable` m68k package index is reachable.
- Debian Ports `unreleased` m68k package index is reachable.
- `qemu-m68k` binfmt execution works in the qualification environment.
- `mmdebstrap` can construct the project current m68k rootfs.
- Qualification output is uploaded as a GitHub Actions artifact.

## What is not yet qualified

- Boot on an Amiga.
- The Amiga kernel artifact.
- m68kDeb installer initramfs runtime.
- Storage/network device support on a specific Amiga profile.
- 68020+MMU runtime behavior.

Those are M1.2/M1.3 and later runtime milestones.
